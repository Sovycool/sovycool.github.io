---
import Starfield from "./Starfield"
import BurgerMenu from "./BurgerMenu"
---
<section class="relative overflow-hidden gradient-hero h-lvh">
  <div class="starfield"></div>
  <Starfield client:load/>
  <!-- NAV -->
  <nav class="fixed top-0 inset-x-0 z-50 p-3">
    <div class="mx-auto max-w-6xl h-0">
      <!-- Desktop: liens alignés à droite -->
      <div class="hidden md:flex justify-end gap-4 text-right absolute right-0 px-5">
        <a href="#about"  class="scroll-down-indicator neon-text px-3 py-2">À propos de moi</a>
        <a href="#projects" class="scroll-down-indicator neon-text px-3 py-2">Mes projets</a>
        <a href="#skills" class="scroll-down-indicator neon-text px-3 py-2">Mes compétences</a>
      </div>

      <!-- Mobile: bouton burger centré -->
      <div class="md:hidden flex justify-center">
        <button
          id="burgerBtn"
          class="glass rounded-xl px-4 py-2 flex items-center gap-3 hover:shadow-neon transition"
          aria-expanded="false"
          aria-controls="mobileMenu"
        >
          <span class="neon-text text-sm tracking-wide">Menu</span>
          <!-- Icône burger -->
          <svg id="burgerIcon" xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                  d="M4 6h16M4 12h16M4 18h16" />
          </svg>
          <!-- Icône close (cachée par défaut) -->
          <svg id="closeIcon" xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 hidden" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                  d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
      </div>

      <!-- Panneau mobile -->
      <div
        id="mobileMenu"
        class="md:hidden mx-auto mt-3 w-[min(92vw,28rem)] glass rounded-2xl overflow-hidden backdrop-blur
              border border-white/10 shadow-lg origin-top scale-95 opacity-0 pointer-events-none
              transition duration-300"
      >
        <ul class="divide-y divide-white/10">
          <li><a href="#about"  class="block text-center px-6 py-4 neon-text">À propos de moi</a></li>
          <li><a href="#projects" class="block text-center px-6 py-4 neon-text">Mes projets</a></li>
          <li><a href="#skills" class="block text-center px-6 py-4 neon-text">Mes compétences</a></li>
          <li><a href="#contacts" class="block text-center px-6 py-4 neon-text">Contacts</a></li>
        </ul>
      </div>
    </div>
  </nav>
  <BurgerMenu client:only />
  <div class="mx-auto max-w-6xl px-6 pt-100 text-center md:text-left">
    <h1 class="text-4xl md:text-6xl font-extrabold neon-text">
      Salut,<br>Moi c'est Sovann !
    </h1>
    <p class="mt-5 max-w-2xl text-lg text-muted">
      Étudiant à EPITECH Mulhouse, passionné par le game dev et les projets
      créatifs. Bienvenue dans ma tête — <span class="neon-text"> entre logique, idées et curiosité.</span>
    </p>
    <div class="mt-8 flex gap-3 justify-center md:justify-start">
      <a href="#contacts" class="px-5 py-3 border border-indigoBrand/40 rounded-[16px] transition shadow-neon">
        Contactez-moi !
      </a>
    </div>
  </div>
</section>