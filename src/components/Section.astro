---
const { id, title, subtitle } = Astro.props;
---
<section id={id} class="mx-auto max-w-6xl px-6 py-16 md:py-24">
  <header class="mb-10">
    <h2 class="text-3xl md:text-4xl font-bold">{title}</h2>
    {subtitle && <p class="text-muted mt-2">{subtitle}</p>}
  </header>
  <slot />
</section>